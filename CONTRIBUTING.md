# 기여 안내

**기여는 계산 오류를 알리거나 어려운 문장을 고치는 것부터 시작할 수 있습니다.** 개발 경험이 없어도 질문이 이해되지 않는 지점, 설명이 부족한 용어, 재현 가능한 계산 문제를 알려 주면 도움이 됩니다.

## 오류는 가상 사례로 알려 주세요

**오류 신고에는 계산기 이름, 가상 입력, 실제 결과, 기대한 결과를 적어 주세요.** 기대한 결과의 근거가 있으면 공식 법령·기관 안내나 직접 계산한 과정을 함께 남겨 주세요.

```text
계산기: 대출 이자 · 상환표 / daechul-ija
사용 환경: Codex 또는 Claude Code, Node.js 버전
가상 입력: 대출 1억원, 연 4%, 1년, 원금은 마지막에 한 번에 상환
실제 결과: 화면에 나온 금액 또는 오류 문구
기대한 결과: 예상한 값과 그 이유
재현 방법: 어떤 질문에 어떻게 답했는지
```

**개인 정보는 공개 이슈에 포함하지 마세요.** 실명, 주민등록번호, 계좌번호, 정확한 주소, 인증 정보, 실제 계약서와 전체 대화 로그는 필요하지 않습니다. 자세한 기준은 [개인정보 안내](docs/privacy.md)를 참고하세요.

## 설명은 결론부터 쉽게 쓰세요

**모든 문장과 문단은 핵심 결론부터 시작합니다.** “예상 세금은 얼마입니다”, “이 조건은 먼저 확인해야 합니다”처럼 사용자가 알아야 할 내용을 앞에 놓고 이유와 조건을 이어 씁니다.

**세금 용어에는 생활 속 뜻을 붙이세요.** “공제”는 계산 대상 금액에서 일정액을 빼는 것, “감면”은 조건을 갖췄을 때 세금을 줄여 주는 것처럼 먼저 설명합니다. 법 조문 번호만으로 사용자 설명을 대신하지 마세요.

**전제와 실제 사실은 구분해서 쓰세요.** “답변을 생략하여 거치기간을 0년으로 계산했습니다”라고 밝혀야 하며, 사용자가 “거치기간이 없다”고 답한 것처럼 바꾸면 안 됩니다.

## 질문과 계산은 함께 수정하세요

**계산기의 문답 규칙은 `catalog/`에서 관리합니다.** 필수 질문에는 임의의 기본값을 넣지 않고, 선택 질문에는 기본값과 사용자가 이해할 설명을 함께 둡니다. 답변에 따라 추가 정보가 필요해지는 경우도 표현해야 합니다.

**스킬과 사용자 안내는 실제 계산 범위를 따라야 합니다.** 지원하지 않는 예외를 지원한다고 쓰거나, 예시 조건을 사용자의 실제 사실로 채우지 마세요. `yangdo-sim` 등 개편안 결과에는 가정 시뮬레이션이라는 표시가 필요합니다.

**계산 코드는 `engine/src/`가 원본이고 `engine/runtime/`이 배포 실행 파일입니다.** 원본을 수정하면 배포 실행 파일을 다시 만들고 일치 여부를 검증해야 합니다. 빌드 절차와 원본 지문은 [계산식 출처와 검증 범위](docs/source-provenance.md)에 있습니다.

## 실행과 검증을 확인하세요

**일반 사용에는 Node.js 22 이상만 필요하며 별도 npm 실행 의존성은 없습니다.** 원본 TypeScript를 바꾸는 개발자는 출처 안내에 기재된 컴파일러도 준비해야 합니다.

**계산기 목록과 질문 정의는 다음 명령으로 확인할 수 있습니다.** 명령은 저장소의 가장 바깥 폴더에서 실행하세요.

```sh
node scripts/calculator.cjs list
node scripts/calculator.cjs describe pyeongsu
node scripts/calculator.cjs questions pyeongsu
```

**면적 환산은 다음 가상 입력으로 실행할 수 있습니다.** 금액을 받는 계산기의 입력은 원 단위이며, 비율과 기간은 각 질문에 표시된 단위를 따릅니다.

```sh
printf '%s\n' '{"value":84,"unit":"sqm"}' | node scripts/calculator.cjs calculate pyeongsu -
```

**변경 검증은 실제 영향을 받는 동작을 확인해야 합니다.** 필수값 누락, 선택값 생략과 전제 표시, 잘못된 단위·범위, 조건별 분기, 계산 경계값을 확인하세요. 세율을 바꾸는 경우에는 해당 시행일과 적용 조건을 함께 검증해야 합니다.

**스킬 생성과 전체 검증은 다음 명령으로 실행합니다.** 카탈로그를 수정했다면 생성된 스킬과 계산기 안내도 함께 갱신해야 합니다.

```sh
node scripts/generate-skills.cjs
node scripts/verify.cjs
```

**자동 검증 통과는 최신 법령 전체를 확인했다는 의미가 아닙니다.** 법령이나 요율을 바꾸면 공식 출처, 적용 시점, 기존 계산과 달라지는 사례를 함께 기록하세요. 자료 목록은 [법령 출처](docs/legal-sources.md)에 있습니다.

**Claude Code의 로컬 검토에는 플러그인 폴더를 직접 지정할 수 있습니다.** 다음 명령은 설치 없이 현재 저장소의 변경을 확인하는 개발용 방식입니다. 제품별 동작은 [공식 플러그인 문서](https://code.claude.com/docs/en/plugins)를 참고하세요.

```sh
claude --plugin-dir .
```

## 변경 이유와 검증 결과를 남기세요

**변경 제안은 해결한 문제부터 설명하세요.** 어떤 입력에서 무엇이 잘못됐는지, 수정 후 어떻게 동작하는지, 어떤 검증을 했는지 적습니다. 확인하지 못한 범위도 함께 남겨 주세요.

**커밋 제목은 변경 이유를 설명하고 필요한 기록은 Git 트레일러로 남깁니다.** 트레일러는 커밋 메시지 끝에 붙이는 `항목: 내용` 형식의 기록입니다.

```text
선택 답변을 생략했을 때 계산 전제를 확인할 수 있게 한다

결과 화면에 생략한 질문의 기본값을 함께 표시한다.

Confidence: high
Scope-risk: narrow
Tested: 선택 질문 생략 후 결과의 전제 표시 확인
Not-tested: 실제 금융회사 약정과의 비교
```

**배포 조건은 MIT 라이선스를 따릅니다.** 외부 코드나 문서를 추가할 때는 출처와 재배포 가능 여부를 확인하고, 필요한 저작권 고지를 보존하세요.
